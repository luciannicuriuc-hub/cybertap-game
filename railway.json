{
  "$schema": "[railway.app](https://railway.app/railway.schema.json)",
  "build": {
    "builder": "NIXPACKS"
  },
  "deploy": {
    "startCommand": "cd backend && npm install && node index.js",
    "restartPolicyType": "ON_FAILURE"
  }
}
